name: benchmark
description: Benchmark for Pureflow and other state management libraries.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  benchmark_harness: any
  pureflow: any
  caffeine: ^3.0.0
  alien_signals: ^2.3.1
  signals_core: ^7.0.0
  bloc: ^9.2.1
  bloc_concurrency: ^0.3.0
  riverpod: ^3.3.2
  mobx: ^2.6.0
  meta: ^1.14.0
  path: ^1.9.0

dev_dependencies:
  build_runner: any
  mobx_codegen: any

